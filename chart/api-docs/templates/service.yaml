apiVersion: v1
kind: Service
metadata:
  name: {{ include "apidocs.fullname" . }}
  labels:
    app: {{ include "apidocs.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "apidocs.name" . }}
